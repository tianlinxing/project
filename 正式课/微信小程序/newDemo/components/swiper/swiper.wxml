<swiper autoplay="{{true}}" indicator-dots="{{true}}" indicator-color="#FF4040" circular="{{true}}">
    <block wx:for="{{data}}">
        <swiper-item>
            <image src="{{item.imgUrl}}" style="width: 100%"></image>
        </swiper-item>
    </block>
</swiper>
